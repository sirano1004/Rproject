genParen <- function(n){
    addParen <- function(res, left, right, cand, count){
        stopofnot(left > 0 & left < right)
        
        if(left == 0 & right == 0){
            append(res, cand)
        }
        else{
            if(left > 0){
                
            }
        }
    }
}

a <- "abcd"
substr(a, 1 , 1) <- 'e'
print(a)


